import React from 'react';
import ReactDOM from 'react-dom/client';
import AdaptDoc from './document/AdaptDoc';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <AdaptDoc />
  </React.StrictMode>
);
